# Resilience metrics comparison

Project comparing resilience metrics from different models.

## Setup
To run this model you need julia v1.9.3. Run `julia --project=.` and then inside the REPL package mode run `instantiate`.
